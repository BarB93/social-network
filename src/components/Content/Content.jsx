import React from 'react';
import ProfilePage from "../../pages/ProfilePage";

const Content = (props) => {
    return (
        <main {...props}>
            <ProfilePage />
        </main>
    )
}

export default Content
